# cparse

A library for datatype conversion and other useful utilities written in C.

## Include

First, clone this repo into your program folder.
To include this library in your program, open your main.c and at the top of the file write: ```#include"cparse/cparse.h"```.